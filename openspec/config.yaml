schema: spec-driven

context: |
  Tech stack: TypeScript, Next.js (App router), Supabase, Oktokit, Tailwind CSS, Vitest
  Domain: Saas edTech platform for learning programming by building projects generated by AI

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
rules:
  proposal:
    - Keep proposals under 500 words
    - Always include a "Non-goals" section
  tasks:
    - Break tasks into chunks of max 2 hours
